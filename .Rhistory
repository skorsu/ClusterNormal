our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
rm(list = ls())
set.seed(2)
K <- 7
n <- 17
xi_vec <- rep(1, K)
ci_vec <- sample(1:2, n, TRUE)
dat <- rnorm(n, c(-5, 5)[ci_vec])
alpha_vec <- rep(1, K)
mu0_vec <- rep(1, K)
a_sigma_vec <- rep(1, K)
b_sigma_vec <- rep(1, K)
lambda_vec <- rep(1, K)
old_assign <- rep(1:4, 28)[1:n]
alpha_vec <- c(rep(1, (K-1)), 0)
our_allocate(old_assign, xi_vec, scale(dat), a_sigma_vec, b_sigma_vec,
lambda_vec, mu0_vec, alpha_vec)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
rm(list = ls())
set.seed(2)
K <- 7
n <- 17
xi_vec <- rep(1, K)
ci_vec <- sample(1:2, n, TRUE)
dat <- rnorm(n, c(-5, 5)[ci_vec])
alpha_vec <- rep(1, K)
mu0_vec <- rep(1, K)
a_sigma_vec <- rep(1, K)
b_sigma_vec <- rep(1, K)
lambda_vec <- rep(1, K)
old_assign <- rep(1:4, 28)[1:n]
alpha_vec <- c(rep(1, (K-1)), 0)
rm(list = ls())
set.seed(2)
K <- 7
n <- 17
xi_vec <- rep(1, K)
ci_vec <- sample(1:2, n, TRUE)
dat <- rnorm(n, c(-5, 5)[ci_vec])
mu0_vec <- rep(1, K)
a_sigma_vec <- rep(1, K)
b_sigma_vec <- rep(1, K)
lambda_vec <- rep(1, K)
old_assign <- rep(1:4, 28)[1:n]
alpha_vec <- c(rep(1, (K-1)), 0)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
which(old_assign %in% c(3, 2)) - 1
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
set.seed(2)
K <- 7
n <- 17
xi_vec <- rep(1, K)
ci_vec <- sample(1:2, n, TRUE)
dat <- rnorm(n, c(-5, 5)[ci_vec])
mu0_vec <- rep(1, K)
a_sigma_vec <- rep(1, K)
b_sigma_vec <- rep(1, K)
lambda_vec <- rep(1, K)
old_assign <- rep(1:4, 28)[1:n]
alpha_vec <- c(rep(1, (K-1)), 0)
our_allocate(old_assign, xi_vec, scale(dat), a_sigma_vec, b_sigma_vec,
lambda_vec, mu0_vec, alpha_vec)
our_allocate(old_assign, xi_vec, scale(dat), a_sigma_vec, b_sigma_vec,
lambda_vec, mu0_vec, alpha_vec)
our_allocate(old_assign, xi_vec, scale(dat), a_sigma_vec, b_sigma_vec,
lambda_vec, mu0_vec, alpha_vec)
our_allocate(old_assign, xi_vec, scale(dat), a_sigma_vec, b_sigma_vec,
lambda_vec, mu0_vec, alpha_vec)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
old_assign
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
our_SM(K, old_assign, alpha_vec, xi_vec, scale(dat), mu0_vec, a_sigma_vec,
b_sigma_vec, lambda_vec, 0.5, 0.5, 10)
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
### Test (3/31/2023): ----------------------------------------------------------
rm(list = ls())
set.seed(2)
rep(seq(1:5)/10, 2)
rm(list = ls())
set.seed(2)
n <- 10
ci_vec <- sample(1:5, n, TRUE)
ci_vec
dat <- rnorm(n)
dat
rep(1:5/2, 2)
1:5/100
6:10/10
rm(list = ls())
set.seed(2)
n <- 10
ci_vec <- sample(1:5, n, TRUE)
dat <- rnorm(n)
mu0_vec <- 1:5/2
a_sigma_vec <- 1:5/100
b_sigma_vec <- 6:10/10
lambda_vec <- c(0.01, 0.1, 1, 10, 100)
marginal_y(ci_vec, dat, mu0_vec, a_sigma_vec, b_sigma_vec, lambda_vec)
rm(list = ls())
set.seed(2)
n <- 10
ci_vec <- sample(1:5, n, TRUE)
dat <- rnorm(n)
mu0_vec <- 1:5/2
a_sigma_vec <- 1:5/100
b_sigma_vec <- 6:10/10
lambda_vec <- c(0.01, 0.1, 1, 10, 100)
marginal_y(ci_vec, dat, mu0_vec, a_sigma_vec, b_sigma_vec, lambda_vec)
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
### Test (3/31/2023): ----------------------------------------------------------
rm(list = ls())
set.seed(2)
n <- 10
ci_vec <- sample(1:5, n, TRUE)
dat <- rnorm(n)
mu0_vec <- 1:5/2
a_sigma_vec <- 1:5/100
b_sigma_vec <- 6:10/10
lambda_vec <- c(0.01, 0.1, 1, 10, 100)
marginal_y(ci_vec, dat, mu0_vec, a_sigma_vec, b_sigma_vec, lambda_vec)
mu0_vec
ci_vec
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
### Required Commands for build the packages: ----------------------------------
uninstall()
compileAttributes()
build()
install()
library(ClusterNormal)
rm(list = ls())
set.seed(2)
n <- 10
ci_vec <- sample(1:5, n, TRUE)
dat <- rnorm(n)
mu0_vec <- 1:5/2
a_sigma_vec <- 1:5/100
b_sigma_vec <- 6:10/10
lambda_vec <- c(0.01, 0.1, 1, 10, 100)
marginal_y(ci_vec, dat, mu0_vec, a_sigma_vec, b_sigma_vec, lambda_vec)
sqrt(2*pi)
log(sqrt(2*pi))
